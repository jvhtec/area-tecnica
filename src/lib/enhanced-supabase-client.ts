// Re-export the main Supabase client
export { 
  supabase, 
  checkNetworkConnection, 
  getRealtimeConnectionStatus 
} from './supabase-client';

// Add any enhanced functionality here, but use the main client instance
