/** dataTransfer MIME type used to identify a job-chip drag (DateHeader -> matrix cell). */
export const MATRIX_JOB_DRAG_MIME = 'application/x-matrix-job-id';

/** dataTransfer MIME type used to identify an assigned-cell move. */
export const MATRIX_ASSIGNMENT_DRAG_MIME = 'application/x-matrix-assignment';
