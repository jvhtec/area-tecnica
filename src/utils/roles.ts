// Wrapper exports for the new roles registry.
// Keeps legacy imports working while enabling code-based roles app-wide.
export {
  roleOptionsForDiscipline,
  labelForCode,
  isRoleCode,
  codeForLabel,
  getDepartmentRoles,
} from '@/types/roles'
