# Vite environment variables (copy this file to ".env" and fill in values)

# Supabase project URL (e.g., https://your-project-id.supabase.co)
VITE_SUPABASE_URL=

# Supabase anon (public) API key
# Optional: if you already have VITE_SUPABASE_PUBLISHABLE_KEY, it will be used as a fallback
VITE_SUPABASE_ANON_KEY=
# VITE_SUPABASE_PUBLISHABLE_KEY=

# Optional flags
# VITE_ENABLE_GPTENG=false

# Web push (VAPID) public key used by the browser client
# Generate a key pair with `npx web-push generate-vapid-keys` and expose
# the public key via this variable. The matching private key must be kept on
# the server and used when sending push notifications.
VITE_VAPID_PUBLIC_KEY=
